// import ytdl from 'ytdl-core';

// const queue = new Map();

// export async function getSongInfo(link: string) {
//   const songInfo: any = await ytdl.getInfo(link);

//   return {
//     title: songInfo.title,
//     url: songInfo.video_url,
//   };
// }

// export async function play(link: string, queue: any) {}
